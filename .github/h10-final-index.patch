diff --git a/index.html b/index.html
index e3ef0d2..f962f8d 100644
--- a/index.html
+++ b/index.html
@@ -19031,7 +19031,10 @@ function ordenarOrdenesPorMonto(lista,criterio){
    Se leen los alias reales del modelo, porque una misma fila puede venir del
    mapeo (estadoRegistro), del crudo de Supabase o de una capa legada. */
 function coiEstadoRegistroDeFila(row){
-  const r=row||{};
+  // filasOrdenesBase() devuelve wrappers { item, ... } en el camino activo.
+  // El estado autoritativo vive en item: leer el wrapper hacia que Archivadas
+  // quedara vacio y que Activas siguiera mostrando filas archivadas.
+  const r=(row&&(row.item||row))||{};
   const v=r.estadoRegistro||r.estado_registro||(r._supabaseRaw&&r._supabaseRaw.estado_registro)||'';
   return String(v==null?'':v).trim().toUpperCase();
 }
@@ -31824,12 +31827,16 @@ throw new Error('El perfil Supabase dejó de estar activo durante la sincronizac
     catch (e) { return true; }
   }
 
-  window.archivarOC = function (referencia) {
+  window.archivarOC = function (referencia, opciones) {
     const ref = referencia || referenciaFicha() ||
       (document.getElementById('f_idObra') && document.getElementById('f_idObra').value) || '';
     const item = itemDe(ref);
     if (item && estaArchivada(item)) return ejecutar(ref, ACTIVO);
-    if (!confirmar('Se va a mover la OC al historial de archivadas.\n\nDeja de figurar en el listado ' +
+    const cfg = opciones && typeof opciones === 'object' ? opciones : {};
+    // H10 puede obtener la confirmacion ANTES de preservar un cierre legacy y
+    // volver por este mismo camino con { confirmado:true }. Asi Cancelar es
+    // realmente side-effect free y no se duplica el dialogo.
+    if (!cfg.confirmado && !confirmar('Se va a mover la OC al historial de archivadas.\n\nDeja de figurar en el listado ' +
       'de activas, pero no se elimina: puede restaurarse cuando quiera.\n\n¿Confirma?')) {
       return Promise.resolve(false);
     }
@@ -32236,6 +32243,29 @@ throw new Error('El perfil Supabase dejó de estar activo durante la sincronizac
     return d.getFullYear() + '-' + p(d.getMonth() + 1) + '-' + p(d.getDate());
   }
 
+  async function leerCierreRemoto(uuid) {
+    const client = typeof window.getSupabaseClient === 'function'
+      ? window.getSupabaseClient()
+      : window.__COI_SUPABASE_CLIENT__;
+    if (!client || typeof client.from !== 'function') {
+      throw new Error('Cliente Supabase no disponible para verificar el cierre actual.');
+    }
+    const consulta = await client.from('coi_ordenes')
+      .select('id,estado_coi,estado_registro,fecha_cierre_operativo,observacion_cierre')
+      .eq('id', uuid).single();
+    if (consulta && consulta.error) throw consulta.error;
+    if (!consulta || !consulta.data) throw new Error('Supabase no devolvió la OC antes del cierre.');
+    return consulta.data;
+  }
+
+  async function refrescarTrasCierreAjeno() {
+    try {
+      if (typeof window.recargarDatosDesdeSupabase === 'function') {
+        await window.recargarDatosDesdeSupabase({ silencioso: true });
+      }
+    } catch (e) {}
+  }
+
   async function cerrar(referencia) {
     if (enCurso) return false;
     const item = itemDe(referencia);
@@ -32263,6 +32293,18 @@ throw new Error('El perfil Supabase dejó de estar activo durante la sincronizac
 
     enCurso = true;
     try {
+      // El snapshot local puede haber quedado viejo mientras otro operador
+      // cerraba la OC. Antes de escribir se relee Supabase y se preserva el
+      // PRIMER cierre confirmado: fecha y observacion no se pisan con una
+      // segunda decision tomada sobre una pantalla desactualizada.
+      const remotoAntes = await leerCierreRemoto(uuid);
+      if (cerradaOperativa(remotoAntes)) {
+        await refrescarTrasCierreAjeno();
+        avisar('La OC ya fue cerrada operativamente por otra actualización. Se conservó el cierre existente.', 'info');
+        sincronizar();
+        return false;
+      }
+
       // Cierre = estado OPERATIVO. Nunca estado_registro: esa columna es el
       // estado de registro y la usa exclusivamente archivar/desarchivar.
       await repo.actualizar(uuid, {
@@ -32363,6 +32405,9 @@ throw new Error('El perfil Supabase dejó de estar activo durante la sincronizac
     }
   }
 
+  const MENSAJE_ARCHIVAR = 'Se va a mover la OC al historial de archivadas.\n\n' +
+    'Deja de figurar en el listado de activas, pero no se elimina: puede restaurarse cuando quiera.\n\n¿Confirma?';
+
   function guardarArchivado(fn, contexto) {
     if (typeof fn !== 'function' || fn.__coiH10Guard) return fn;
     const envuelto = async function (referencia) {
@@ -32374,6 +32419,17 @@ throw new Error('El perfil Supabase dejó de estar activo durante la sincronizac
       // Solo cuando la OC va HACIA el historial. Desarchivar no destruye nada.
       const item = itemDe(ref);
       if (item && !estaArchivada(item)) {
+        // En la accion UI la confirmacion sucede ANTES de cualquier escritura.
+        // H09 acepta {confirmado:true} para no preguntar dos veces al retomar
+        // el camino canonico. Cancelar, por lo tanto, no canonicaliza ni archiva.
+        if (contexto === 'window.archivarOC') {
+          if (!confirmar(MENSAJE_ARCHIVAR)) return false;
+          const preservado = await canonicalizarCierreLegacy(ref);
+          if (!preservado) return false;
+          return fn.call(this, ref, { confirmado: true });
+        }
+        // La API programatica de H09 no mostraba confirmacion; conserva ese
+        // contrato, pero pasa por la misma preservacion del cierre legacy.
         const preservado = await canonicalizarCierreLegacy(ref);
         if (!preservado) return false;
       }
@@ -32546,7 +32602,7 @@ throw new Error('El perfil Supabase dejó de estar activo durante la sincronizac
      #inicio            #ordenes/activas|archivadas|todas   #calendario
      #alertas           #red            #estacion/<nombre>  #carga
      #um  #um/servicios #buscador       #administracion     #acerca
-     #ficha-oc/<nro>[/<subpestaña>]     #ficha-um/<id>
+     #timeline          #ficha-oc/<nro>[/<subpestaña>]      #ficha-um/<id>
 
    La subpestaña de la Ficha OC usa los siete paneles reales: resumen,
    contractual, certificaciones, financiero, documentos, fotos y observaciones.
@@ -32574,6 +32630,7 @@ throw new Error('El perfil Supabase dejó de estar activo durante la sincronizac
     ['um', 'vistaUnidadesMantenimiento'],
     ['carga', 'vistaCarga'],
     ['buscador', 'vistaBuscador'],
+    ['timeline', 'vistaTimelineCOI'],
     ['administracion', 'vistaAdministracionSistema'],
     ['acerca', 'vistaAcercaSistema'],
     ['vencimientos', 'vistaCalendarioVencimientos'],
@@ -32599,6 +32656,20 @@ throw new Error('El perfil Supabase dejó de estar activo durante la sincronizac
   const MODOS_REGISTRO = ['activas', 'archivadas', 'todas'];
 
   // ---------------------------------------------------------- resolucion
+  function identidadOC(v) { return texto(v).toUpperCase(); }
+
+  function coincideOCExacta(item, referencia) {
+    const i = item || {};
+    const raw = i._supabaseRaw || {};
+    const buscada = identidadOC(referencia);
+    if (!buscada) return false;
+    const ids = [
+      i.id, i.supabaseId, i.idOC, i.idObra, i.id_obra, i.numeroOC, i.nro_oc, i.oc,
+      raw.id, raw.id_obra, raw.nro_oc
+    ];
+    return ids.some((v) => identidadOC(v) === buscada);
+  }
+
   function itemDe(referencia) {
     if (!texto(referencia)) return null;
     const candidatos = [];
@@ -32607,7 +32678,10 @@ throw new Error('El perfil Supabase dejó de estar activo durante la sincronizac
     try { if (typeof window.buscarPorIdObra === 'function') candidatos.push(window.buscarPorIdObra(referencia)); } catch (e) {}
     for (const c of candidatos) {
       const item = c && (c.item || c);
-      if (item && typeof item === 'object' && Object.keys(item).length) return item;
+      // Algunos resolvers legados hacen coincidencia por substring. Para una
+      // URL eso es peligroso: #ficha-oc/4530 no puede abrir la primera OC que
+      // empiece con 4530. La identidad de una ruta siempre es exacta.
+      if (item && typeof item === 'object' && Object.keys(item).length && coincideOCExacta(item, referencia)) return item;
     }
     return null;
   }
@@ -32718,12 +32792,28 @@ throw new Error('El perfil Supabase dejó de estar activo durante la sincronizac
   function fijarRutaError(ruta) { rutaError = texto(ruta); }
   function limpiarRutaError() { rutaError = ''; limpiarDOMEstadoRuta(); }
 
+  function identidadErrorVisible(vista) {
+    const errorVisible = vista === 'vistaFichaOC' && IDS_ESTADO_RUTA.some((id) => {
+      const n = document.getElementById(id);
+      return Boolean(n && n.isConnected);
+    });
+    if (!errorVisible) return '';
+    if (rutaError) return rutaError;
+    return '';
+  }
+
   // La ruta que describe lo que hay en pantalla ahora mismo.
   function rutaVigente() {
-    // Un estado de error manda sobre cualquier derivacion: es lo que hay en
-    // pantalla, y su direccion tiene que sobrevivir al refresco.
-    if (rutaError) return rutaError;
     const vista = vistaActiva();
+    // Un estado de error manda SOLO mientras ese estado es realmente la
+    // pantalla visible. Si el operador sale por el menu, buscador u otro
+    // control ordinario, la identidad vieja se descarta y el nuevo modulo
+    // puede publicar su propia URL.
+    if (rutaError) {
+      const identidad = identidadErrorVisible(vista);
+      if (identidad) return identidad;
+      limpiarRutaError();
+    }
     if (vista === 'vistaFichaOC') {
       const nro = ocEnPantalla();
       if (!nro) return '';
@@ -32852,7 +32942,11 @@ throw new Error('El perfil Supabase dejó de estar activo durante la sincronizac
   }
 
   function mostrarRutaInvalida(hashCrudo) {
-    fijarRutaError('');
+    // Conserva la identidad CRUDA pedida. Si se viene de una ficha valida,
+    // ocActualId sigue apuntando a ella; sin identidad propia, la normalizacion
+    // reemplazaria el hash roto por la OC anterior aunque la pantalla muestre
+    // «ruta invalida».
+    fijarRutaError(texto(hashCrudo));
     pintarEstadoRuta('h10RutaInvalida',
       'No se pudo interpretar la dirección solicitada.',
       'La dirección <b>' + escHtml('#' + texto(hashCrudo)) + '</b> está mal formada y no ' +
@@ -33020,6 +33114,13 @@ throw new Error('El perfil Supabase dejó de estar activo durante la sincronizac
         else abrirVista('vistaFichaUM');
         return;
       }
+      if (cabeza === 'timeline') {
+        // Timeline crea su vista dinamicamente. mostrarVista() por si sola no
+        // alcanza si la seccion todavia no existe en un acceso directo/F5.
+        if (typeof window.abrirTimelineCOI === 'function') window.abrirTimelineCOI({});
+        else abrirVista('vistaTimelineCOI');
+        return;
+      }
       // #red es la Red GENERAL. Sin limpiar el contexto, estacionVigente y
       // #panelEstacion.active sobrevivian a la navegacion y rutaVigente()
       // volvia a derivar #estacion/<nombre>, reemplazando el #red recien
@@ -33031,10 +33132,21 @@ throw new Error('El perfil Supabase dejó de estar activo durante la sincronizac
       }
       if (cabeza === 'estacion') {
         const nombre = decodificados[1] || '';
+        // Resolver una estacion nueva nunca hereda la seleccion anterior. Si
+        // el nombre fue renombrado o no existe, se cae de forma segura a la Red
+        // general en vez de volver silenciosamente a la estacion A.
+        limpiarEstacion();
         abrirVista('vistaRed');
         const norm = (v) => texto(v).normalize('NFD').replace(/[\u0300-\u036f]/g, '').toUpperCase();
         const est = listaEstaciones().find((e) => norm(e && e.nombre) === norm(nombre));
-        if (est && typeof window.selectStation === 'function') window.selectStation(est);
+        if (est && typeof window.selectStation === 'function') {
+          window.selectStation(est);
+        } else if (nombre) {
+          try {
+            const toast = window.coiToast || window.toast;
+            if (typeof toast === 'function') toast('No se encontró la estación solicitada. Se muestra la Red general.', 'warning');
+          } catch (e) {}
+        }
         return;
       }
       if (cabeza === 'ordenes') {
@@ -33144,6 +33256,10 @@ throw new Error('El perfil Supabase dejó de estar activo durante la sincronizac
     // El resultado de esperarArranque SI se mira: sin datos autoritativos no
     // se aplica una ruta que terminaria afirmando que la entidad no existe.
     const listo = await esperarArranque();
+    // esperarArranque() es asincrono. Si durante esa espera el operador ya
+    // navego, la ruta capturada al inicio quedo obsoleta y NO puede volver a
+    // imponerse sobre su accion mas reciente.
+    if (hashActual() !== inicial) return;
     if (!listo) {
       const partes = texto(inicial).split('/').filter((x) => x !== '');
       const cabeza = safeDecode(partes[0] || '');
