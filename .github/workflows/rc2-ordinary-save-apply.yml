name: RC2 Ordinary Save Patch

on:
  push:
    branches:
      - hotfix/rc2-ordinary-save
    paths:
      - .coi-qa/apply-rc2-ordinary-save-hotfix.mjs
      - .github/workflows/rc2-ordinary-save-apply.yml

permissions:
  contents: write

jobs:
  apply-patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout rama hotfix
        uses: actions/checkout@v4
        with:
          ref: hotfix/rc2-ordinary-save
          fetch-depth: 0

      - name: Configurar Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Aplicar sustitución exacta
        run: node .coi-qa/apply-rc2-ordinary-save-hotfix.mjs

      - name: Validar diff
        run: |
          git diff --check
          git diff --exit-code -- . ':!index.html' || true
          test "$(git diff --numstat -- index.html | wc -l)" -eq 1

      - name: Commit del hotfix generado
        run: |
          if git diff --quiet -- index.html; then
            echo "No hay cambios que confirmar"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          git commit -m "fix(frontend): delegar edición ordinaria al editor RC2"
          git push origin HEAD:hotfix/rc2-ordinary-save
