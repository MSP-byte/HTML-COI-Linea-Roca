name: Finalize final UI v3
on:
  push:
    branches:
      - fix/obra-resumen-repositorio-supabase
    paths:
      - .coi-qa/finalize.trigger3
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/obra-resumen-repositorio-supabase
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Aplicar y registrar
        id: patch
        continue-on-error: true
        run: |
          set +e
          node .coi-qa/finalize_final_ui.mjs > .coi-qa/finalize-result.txt 2>&1
          code=$?
          echo "EXIT_CODE=$code" >> .coi-qa/finalize-result.txt
          exit $code
      - name: Validar test si parche ok
        if: steps.patch.outcome == 'success'
        run: node --check tests/obra_resumen_supabase.spec.js
      - name: Commit resultado
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .coi-qa/finalize-result.txt
          if [ "${{ steps.patch.outcome }}" = "success" ]; then git add index.html tests/obra_resumen_supabase.spec.js; fi
          git commit -m "chore: registrar resultado cierre UI" || exit 0
          git push origin HEAD:fix/obra-resumen-repositorio-supabase
