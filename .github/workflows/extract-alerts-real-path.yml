name: Extract alerts real path

on:
  push:
    branches: [fix/alerts-collapsible-init]

permissions:
  contents: write

jobs:
  extract:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/alerts-collapsible-init
          persist-credentials: true
      - name: Extract alert render contexts
        run: |
          python3 - <<'PY'
          from pathlib import Path
          s=Path('index.html').read_text(encoding='utf-8')
          lines=s.splitlines()
          terms=['renderCentroAlertasFix','renderCentroAlertas','btnCentroAlertas','coi-alertas-table','alertasTbody','vistaCentroAlertas']
          out=[]
          for term in terms:
              out.append(f'### {term}')
              hits=[i for i,l in enumerate(lines) if term in l]
              out.append(f'hits={len(hits)}')
              for i in hits[:30]:
                  a=max(0,i-18); b=min(len(lines),i+32)
                  out.append(f'-- around line {i+1} --')
                  out.extend(f'{j+1}: {lines[j]}' for j in range(a,b))
                  out.append('')
          Path('.github/alerts_real_path_context.txt').write_text('\n'.join(out),encoding='utf-8')
          PY
      - name: Commit context
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .github/alerts_real_path_context.txt
          git commit -m "chore: capture real alerts render path"
          git push origin HEAD:fix/alerts-collapsible-init
