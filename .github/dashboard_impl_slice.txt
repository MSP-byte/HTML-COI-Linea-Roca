23260:   function stateOf(row,item){
23261:     if(typeof window.v60CalcularEstadoVigencia==='function'){try{return window.v60CalcularEstadoVigencia(item);}catch(e){}}
23262:     const raw=fold(row?.estadoVigenciaOperativa||item?.estadoVigenciaOperativa||row?.estado||item?.estado);
23263:     if(raw.includes('CERRAD')||raw.includes('FINALIZAD')||raw.includes('ARCHIVAD'))return 'Cerrada';
23264:     const days=row?.diasRestantes??daysFromToday(row?.fin||itemValue(item,['fechaFin','fechaVencimiento','vencimiento','fin']));
23265:     if(days!==null&&days<0)return raw.includes('SALDO')||raw.includes('CERTIFICABLE')?'Vencida certificable por saldo remanente':'Vencida no certificable';
23266:     if(days!==null&&days<=30)return 'Próxima a vencer';
23267:     return 'Vigente';
23268:   }
23269:   function statusKey(record){const s=fold(record.contractStatus);if(s.includes('CERRAD')||s.includes('FINALIZAD')||s.includes('ARCHIVAD'))return'closed';if(s.includes('VENCIDA')&&(s.includes('SALDO')||s.includes('CERTIFICABLE')||s.includes('HABILITADA')))return'certifiable';if(s.includes('VENCIDA'))return'expired';if(s.includes('PROXIMA'))return'upcoming';return'execution';}
23270:   function itemAmount(item){
23271:     if(typeof window.obtenerMontoOCV52==='function'){try{const value=Number(window.obtenerMontoOCV52(item));if(Number.isFinite(value)&&value)return value;}catch(e){}}
23272:     return number(itemValue(item,['montoTotalOCOriginal','montoTotalOC','montoOC','monto','SALDO_OC_TOTAL','saldoOcTotal','importe','MONTO']));
23273:   }
23274:   function financialFor(record){
23275:     const item=record.item||{};const currency=fold(itemValue(item,['moneda','MONEDA','currency'])||'ARS');
23276:     if(currency&&currency!=='ARS'&&!currency.includes('PESO'))return{currency,total:0,used:0,balance:0,missing:false,excluded:true,inconsistent:false};
23277:     let total=0,used=0,balance=0,positions=0,negative=false;
23278:     if(typeof window.calcularResumenFinancieroOC==='function'&&record.oc&&record.oc!=='SIN OC'){
23279:       try{const r=window.calcularResumenFinancieroOC(record.oc)||{};total=number(r.totalPosiciones);used=number(r.totalCertificado);balance=number(r.saldo??r.totalLibre);positions=number(r.cantidadPosicionesOC);if(Array.isArray(r.libres))negative=r.libres.some(p=>number(p.saldoMonto)<-.01||number(p.saldoCantidad)<-.0001);}catch(e){}
23280:     }
23281:     if(!total){total=itemAmount(item);used=number(itemValue(item,['montoConsumidoCertificado','montoConsumido','saldoEjecutado','SALDO_EJECUTADO']));balance=number(itemValue(item,['saldoFinancieroPendiente','saldoDisponible','saldoRemanente','saldoOcTotal']));if(!balance&&total)balance=Math.max(0,total-used);}
23282:     const missing=!(total>0);const inconsistent=negative||used>total+.01||balance<-.01;
23283:     return{currency:'ARS',total,used,balance,positions,missing,excluded:false,inconsistent};
23284:   }
23285:   function collectRecords(){
23286:     let rows=[];try{rows=typeof window.todasLasOC==='function'?window.todasLasOC():[];}catch(e){console.error('Dashboard V33: no se pudieron leer las OC',e);}
23287:     if(!Array.isArray(rows))rows=[];
23288:     const map=new Map();
23289:     rows.forEach((row,index)=>{
23290:       const item=row?.item||row||{};const key=ocKey(row,item,index);const station=clean(row?.estacion||itemValue(item,['estacion','ESTACION']))||'Sin estación';const branch=clean(row?.ramal||itemValue(item,['ramal','RAMAL']))||'';
23291:       let rec=map.get(key);
23292:       if(!rec){
23293:         const end=parseDate(row?.fin||itemValue(item,['fechaFin','FECHA_FIN','fechaVencimiento','vencimiento','fin']));
23294:         rec={key,oc:ocNumber(row,item),item,type:typeOf(row,item),provider:clean(row?.proveedor||itemValue(item,['proveedor','PROVEEDOR']))||'Sin proveedor',state:clean(row?.estado||item?.estado)||'Sin estado',contractStatus:stateOf(row,item),end,start:parseDate(row?.inicio||itemValue(item,['fechaInicio','FECHA_INICIO','fechaActaInicio','actaInicio','inicio'])),nextCert:parseDate(row?.proximaCertificacion||itemValue(item,['proximaCertificacion','proxCertificacion','PROX_CERTIFICACION','fechaProximaCertificacion'])),stations:new Set(),branches:new Set(),rows:[]};map.set(key,rec);
23295:       }
23296:       rec.stations.add(station);if(branch)rec.branches.add(branch);rec.rows.push(row);
23297:       if(!rec.end)rec.end=parseDate(row?.fin);if(!rec.nextCert)rec.nextCert=parseDate(row?.proximaCertificacion);
23298:     });
23299:     return [...map.values()].map(rec=>{rec.station=[...rec.stations][0]||'Sin estación';rec.branch=[...rec.branches][0]||'';rec.daysToEnd=rec.end?Math.round((rec.end-today())/86400000):null;rec.statusKey=statusKey(rec);rec.financial=financialFor(rec);return rec;});
23300:   }
23301:   function filterRecords(records){
23302:     const f=state.filters;
23303:     return records.filter(record=>(!f.type||record.type===f.type)&&(!f.branch||record.branches.has(f.branch))&&(!f.provider||record.provider===f.provider)&&(!f.station||record.stations.has(f.station))&&(!f.status||record.statusKey===f.status));
23304:   }
23305:   function periodRange(){const p=PERIODS[state.filters.period];const start=today();if(p===null)return null;if(p==='year')return{start:new Date(start.getFullYear(),0,1),end:new Date(start.getFullYear(),11,31)};return{start,end:new Date(start.getFullYear(),start.getMonth(),start.getDate()+Number(p||0))};}
23306:   function inRange(value,includeOverdue=false){const d=parseDate(value);if(!d)return false;const range=periodRange();if(!range)return true;if(includeOverdue&&d<range.start)return true;return d>=range.start&&d<=range.end;}
23307:   function recordSet(records){return new Set(records.map(r=>fold(r.oc)));}
23308:   function collectEvents(records){
23309:     let raw=[];try{raw=typeof window.generarEventosCalendarioCOI==='function'?window.generarEventosCalendarioCOI():[];}catch(e){console.warn('Dashboard V33: calendario no disponible',e);}
23310:     const allowed=recordSet(records),seen=new Set();return (Array.isArray(raw)?raw:[]).filter(event=>{const oc=fold(event.oc||event.item?.oc||event.item?.numeroOC);if(allowed.size&&oc&&!allowed.has(oc))return false;const key=[oc,fold(event.tipoEvento),iso(parseDate(event.fecha))].join('|');if(seen.has(key))return false;seen.add(key);return true;});
23311:   }
23312:   function collectAlerts(records){
23313:     let raw=[];try{raw=typeof window.generarAlertasCOI==='function'?window.generarAlertasCOI():[];}catch(e){console.warn('Dashboard V33: alertas no disponibles',e);}
23314:     const allowed=recordSet(records),seen=new Set();return (Array.isArray(raw)?raw:[]).filter(alert=>{const oc=fold(alert.oc||alert.item?.oc||alert.item?.numeroOC);if(allowed.size&&oc&&!allowed.has(oc))return false;const key=alert.id||[oc,fold(alert.tipoAlerta),alert.fechaRelacionada].join('|');if(seen.has(key))return false;seen.add(key);return true;});
23315:   }
23316:   function collectTimeline(records){const allowed=recordSet(records);const rows=Array.isArray(window.coiTimelineEvents)?window.coiTimelineEvents:[];return rows.filter(event=>!allowed.size||!event.oc||allowed.has(fold(event.oc))).slice().sort((a,b)=>String(`${b.fecha||''} ${b.hora||''}`).localeCompare(String(`${a.fecha||''} ${a.hora||''}`)));}
23317:   function create(tag,className,text){const el=document.createElement(tag);if(className)el.className=className;if(text!==undefined)el.textContent=String(text);return el;}
23318:   function option(select,value,label){const o=document.createElement('option');o.value=value;o.textContent=label;select.appendChild(o);}
23319:   function fillSelect(id,values,placeholder,current){const select=$(id);if(!select)return;select.textContent='';option(select,'',placeholder);values.forEach(value=>option(select,value,value));select.value=values.includes(current)?current:'';}
23320:   function skeleton(){
23321:     const mount=$('dashboardInteractivoMount');if(!mount)return;
23322:     mount.innerHTML=`<div class="d33-shell"><div class="d33-head"><div><h3>Resumen operativo Línea Roca</h3><p>Estado contractual, agenda COI, ejecución financiera y prioridades accionables.</p></div><span class="d33-updated" id="d33Updated">Actualizando...</span></div><div class="d33-filters" id="d33Filters"><div class="d33-field"><label for="d33Period">Período</label><select id="d33Period"><option value="today">Hoy</option><option value="30">Próximos 30 días</option><option value="60">Próximos 60 días</option><option value="90">Próximos 90 días</option><option value="180">Próximos 180 días</option><option value="year">Año actual</option><option value="all">Todo</option></select></div><div class="d33-field"><label for="d33Type">Tipo</label><select id="d33Type"></select></div><div class="d33-field"><label for="d33Branch">Ramal</label><select id="d33Branch"></select></div><div class="d33-field"><label for="d33Provider">Proveedor</label><select id="d33Provider"></select></div><div class="d33-field"><label for="d33Station">Estación</label><select id="d33Station"></select></div><div class="d33-field"><label for="d33Status">Estado contractual</label><select id="d33Status"></select></div><button type="button" class="d33-clear" id="d33Clear">Limpiar filtros</button></div><section class="d33-kpis" id="d33Kpis"></section><section class="d33-grid-2"><article class="d33-card"><div class="d33-card-head"><div><h4>Estado contractual de las OC</h4><p>Distribución de la cartera filtrada.</p></div><button type="button" id="d33ViewNetwork">Ver en Red</button></div><div id="d33Donut"></div></article><article class="d33-card"><div class="d33-card-head"><div><h4>Agenda COI — próximos seis meses</h4><p>Certificaciones, vencimientos y actas.</p></div><button type="button" id="d33OpenCalendar">Abrir Calendario</button></div><div id="d33Agenda"></div></article></section><article class="d33-card"><div class="d33-card-head"><div><h4>Ejecución financiera</h4><p>Consolidado ARS de las OC filtradas.</p></div><span id="d33FinanceState"></span></div><div id="d33Finance"></div></article><section class="d33-grid-2 equal"><article class="d33-card"><div class="d33-card-head"><div><h4>Prioridades de hoy</h4><p>Acciones ordenadas por severidad y proximidad.</p></div><button type="button" id="d33OpenAlerts">Centro de Alertas</button></div><div id="d33Priorities"></div></article><article class="d33-card"><div class="d33-card-head"><div><h4>Actividad reciente</h4><p>Últimos movimientos del Timeline COI.</p></div><button type="button" id="d33OpenTimeline">Ver Timeline COI</button></div><div id="d33Activity"></div></article></section><section class="d33-secondary"><article class="d33-card"><div class="d33-card-head"><div><h4>Resumen operativo secundario</h4><p>Indicadores descriptivos de la cartera actual.</p></div></div><div class="d33-chips" id="d33Secondary"></div></article><article class="d33-card"><div class="d33-card-head"><div><h4>Estaciones con mayor actividad</h4><p>Ranking por cantidad de OC asociadas.</p></div></div><div class="d33-ranking" id="d33Ranking"></div></article></section></div><div class="d33-tooltip" id="d33Tooltip" role="tooltip"></div>`;
23323:   }
23324:   function renderFilters(records){
23325:     $('d33Period').value=state.filters.period;
23326:     fillSelect('d33Type',unique(records.map(r=>r.type)).sort(),'Todos los tipos',state.filters.type);
23327:     fillSelect('d33Branch',unique(records.flatMap(r=>[...r.branches])).sort(),'Todos los ramales',state.filters.branch);
23328:     fillSelect('d33Provider',unique(records.map(r=>r.provider)).sort(),'Todos los proveedores',state.filters.provider);
23329:     fillSelect('d33Station',unique(records.flatMap(r=>[...r.stations])).sort(),'Todas las estaciones',state.filters.station);
23330:     const status=$('d33Status');status.textContent='';option(status,'','Todos los estados');Object.entries(STATUS).forEach(([key,cfg])=>option(status,key,cfg.label));status.value=state.filters.status;
23331:   }
23332:   function horizonLabel(){return state.filters.period==='today'?'hoy':state.filters.period==='all'?'todo el período':state.filters.period==='year'?'el año actual':`los próximos ${state.filters.period} días`;}
23333:   function renderKpis(records,alerts){
23334:     const active=records.filter(r=>['execution','upcoming','certifiable'].includes(r.statusKey));
23335:     const upcoming=records.filter(r=>r.daysToEnd!==null&&r.daysToEnd>=0&&inRange(r.end));
23336:     const cert=records.filter(r=>r.statusKey==='certifiable');
23337:     const criticalAlerts=alerts.filter(a=>{const sev=fold(a.severidad||a.categoria);const type=fold(a.tipoAlerta||a.mensaje);return sev.includes('ROJA')||sev.includes('CRIT')||type.includes('DOCUMENT')||type.includes('EXPEDIENTE');});
23338:     const types=active.reduce((acc,r)=>(acc[r.type]=(acc[r.type]||0)+1,acc),{});
23339:     const data=[
23340:       {value:active.length,label:'OC activas',sub:`${types.Obra||0} Obras · ${types.Servicio||0} Servicios`,kind:'',action:'active'},
23341:       {value:upcoming.length,label:'Próximas a vencer',sub:`Dentro de ${horizonLabel()}`,kind:'warn',action:'upcoming'},
23342:       {value:cert.length,label:'Vencidas con saldo',sub:'Continúan certificables bajo la misma OC',kind:'cert',action:'certifiable'},
23343:       {value:criticalAlerts.length,label:'Alertas críticas / documentales',sub:'Requieren intervención o revisión',kind:'bad',action:'alerts'}
23344:     ];
23345:     const host=$('d33Kpis');host.textContent='';data.forEach(k=>{const card=create('article',`d33-kpi ${k.kind}`);card.tabIndex=0;card.setAttribute('role','button');card.dataset.action=k.action;const value=create('b','d33-kpi-value',k.value);const label=create('span','d33-kpi-label',k.label);const sub=create('span','d33-kpi-sub',k.sub);const open=create('span','d33-kpi-open','Abrir');card.append(value,label,sub,open);host.appendChild(card);});
23346:   }
23347:   function showTooltip(event,text){const tip=$('d33Tooltip');if(!tip)return;tip.textContent=text;tip.style.display='block';tip.style.left=Math.min(window.innerWidth-300,event.clientX+14)+'px';tip.style.top=Math.min(window.innerHeight-100,event.clientY+14)+'px';}
23348:   function hideTooltip(){const tip=$('d33Tooltip');if(tip)tip.style.display='none';}
23349:   function renderDonut(records){
23350:     const host=$('d33Donut');host.textContent='';const counts=Object.keys(STATUS).map(key=>({key,...STATUS[key],value:records.filter(r=>r.statusKey===key).length}));const total=counts.reduce((a,c)=>a+c.value,0);
23351:     if(!total){host.appendChild(create('div','d33-empty','No existen OC para los filtros seleccionados.'));return;}
23352:     const layout=create('div','d33-donut-layout');const ns='http://www.w3.org/2000/svg',svg=document.createElementNS(ns,'svg');svg.setAttribute('viewBox','0 0 260 260');svg.classList.add('d33-svg','d33-donut-svg');svg.setAttribute('aria-label',`Estado contractual de ${total} OC`);const radius=88,circ=2*Math.PI*radius;let offset=0;
23353:     const base=document.createElementNS(ns,'circle');base.setAttribute('cx','130');base.setAttribute('cy','130');base.setAttribute('r',radius);base.setAttribute('fill','none');base.setAttribute('stroke','#edf2f6');base.setAttribute('stroke-width','27');svg.appendChild(base);
23354:     counts.forEach(c=>{if(!c.value)return;const length=c.value/total*circ;const circle=document.createElementNS(ns,'circle');circle.setAttribute('cx','130');circle.setAttribute('cy','130');circle.setAttribute('r',radius);circle.setAttribute('fill','none');circle.setAttribute('stroke',c.color);circle.setAttribute('stroke-width','27');circle.setAttribute('stroke-linecap','butt');circle.setAttribute('stroke-dasharray',`${length} ${circ-length}`);circle.setAttribute('stroke-dashoffset',String(-offset));circle.setAttribute('transform','rotate(-90 130 130)');circle.classList.add('d33-segment');circle.tabIndex=0;circle.dataset.status=c.key;circle.setAttribute('role','button');circle.setAttribute('aria-label',`${c.label}: ${c.value}, ${Math.round(c.value/total*100)} por ciento`);circle.addEventListener('pointermove',ev=>showTooltip(ev,`${c.label}: ${c.value} OC (${Math.round(c.value/total*100)}%)`));circle.addEventListener('pointerleave',hideTooltip);circle.addEventListener('click',()=>openOrders({status:c.key}));circle.addEventListener('keydown',ev=>{if(ev.key==='Enter'||ev.key===' '){ev.preventDefault();openOrders({status:c.key});}});svg.appendChild(circle);offset+=length;});
23355:     const big=document.createElementNS(ns,'text');big.setAttribute('x','130');big.setAttribute('y','127');big.classList.add('d33-donut-center-big');big.textContent=total;const small=document.createElementNS(ns,'text');small.setAttribute('x','130');small.setAttribute('y','148');small.classList.add('d33-donut-center-small');small.textContent='OC TOTALES';svg.append(big,small);
23356:     const legend=create('div','d33-legend');counts.forEach(c=>{const row=create('div','d33-legend-item');row.tabIndex=0;row.setAttribute('role','button');row.dataset.status=c.key;const sw=create('i','d33-swatch');sw.style.background=c.color;const label=create('div');label.append(create('b','',c.label),create('span','',`${total?Math.round(c.value/total*100):0}% de la cartera`));const val=create('strong','',c.value);row.append(sw,label,val);row.addEventListener('click',()=>openOrders({status:c.key}));row.addEventListener('keydown',ev=>{if(ev.key==='Enter'||ev.key===' '){ev.preventDefault();openOrders({status:c.key});}});legend.appendChild(row);});layout.append(svg,legend);host.appendChild(layout);
23357:   }
23358:   function eventSeries(event){const type=fold(event.tipoEvento);if(type.includes('CERTIFIC'))return'cert';if(type.includes('VENCIMIENTO'))return'vto';if(type.includes('AVANCE')||type.includes('ACTA')||type.includes('MEDICION'))return'acta';return'';}
23359:   function agendaData(events){
23360:     const start=today(),months=[];for(let i=0;i<6;i++){const date=new Date(start.getFullYear(),start.getMonth()+i,1);months.push({date,key:`${date.getFullYear()}-${String(date.getMonth()+1).padStart(2,'0')}`,label:new Intl.DateTimeFormat('es-AR',{month:'short'}).format(date).replace('.','').toUpperCase(),values:{cert:0,vto:0,acta:0},types:{}});}
23361:     events.forEach(event=>{const date=parseDate(event.fecha);const series=eventSeries(event);if(!date||!series)return;const key=`${date.getFullYear()}-${String(date.getMonth()+1).padStart(2,'0')}`,month=months.find(m=>m.key===key);if(month){month.values[series]++;month.types[series]=event.tipoEvento;}});return months;
23362:   }
23363:   function renderAgenda(events){
23364:     const host=$('d33Agenda');host.textContent='';const data=agendaData(events),max=Math.max(1,...data.flatMap(m=>Object.values(m.values)));const ns='http://www.w3.org/2000/svg',svg=document.createElementNS(ns,'svg');svg.setAttribute('viewBox','0 0 760 320');svg.classList.add('d33-svg','d33-agenda-svg');svg.setAttribute('aria-label','Agenda COI de los próximos seis meses');const left=44,right=18,top=20,bottom=55,width=760-left-right,height=320-top-bottom,group=width/6,barW=Math.min(17,(group-20)/AGENDA.length);
23365:     for(let i=0;i<=4;i++){const y=top+height-height*i/4;const line=document.createElementNS(ns,'line');line.setAttribute('x1',left);line.setAttribute('x2',760-right);line.setAttribute('y1',y);line.setAttribute('y2',y);line.classList.add('d33-axis');svg.appendChild(line);const label=document.createElementNS(ns,'text');label.setAttribute('x',left-8);label.setAttribute('y',y+4);label.setAttribute('text-anchor','end');label.classList.add('d33-axis-label');label.textContent=Math.round(max*i/4);svg.appendChild(label);}
23366:     data.forEach((month,mi)=>{const gx=left+mi*group+(group-(barW*AGENDA.length+3*(AGENDA.length-1)))/2;AGENDA.forEach((series,si)=>{const value=month.values[series.key],h=value/max*height,x=gx+si*(barW+3),y=top+height-h;const bar=document.createElementNS(ns,'rect');bar.setAttribute('x',x);bar.setAttribute('y',y);bar.setAttribute('width',barW);bar.setAttribute('height',Math.max(value?3:0,h));bar.setAttribute('fill',series.color);bar.classList.add('d33-bar');bar.tabIndex=0;bar.setAttribute('role','button');bar.setAttribute('aria-label',`${month.label}: ${series.label}, ${value}`);bar.addEventListener('pointermove',ev=>showTooltip(ev,`${month.label}: ${series.label} — ${value}`));bar.addEventListener('pointerleave',hideTooltip);bar.addEventListener('click',()=>openCalendar(month.date,month.types[series.key]||series.label));bar.addEventListener('keydown',ev=>{if(ev.key==='Enter'||ev.key===' '){ev.preventDefault();openCalendar(month.date,month.types[series.key]||series.label);}});svg.appendChild(bar);});const label=document.createElementNS(ns,'text');label.setAttribute('x',left+mi*group+group/2);label.setAttribute('y',top+height+25);label.classList.add('d33-month-label');label.textContent=month.label;svg.appendChild(label);});host.appendChild(svg);
23367:     const legend=create('div','d33-agenda-legend');AGENDA.forEach(series=>{const pill=create('span');const sw=create('i','d33-swatch');sw.style.background=series.color;pill.append(sw,document.createTextNode(series.label));legend.appendChild(pill);});host.appendChild(legend);
23368:   }
23369:   function renderFinance(records){
23370:     const included=records.map(r=>r.financial).filter(f=>!f.excluded),excluded=records.filter(r=>r.financial.excluded).length;const total=included.reduce((a,f)=>a+f.total,0),used=included.reduce((a,f)=>a+f.used,0),balance=included.reduce((a,f)=>a+Math.max(0,f.balance),0),missing=included.filter(f=>f.missing).length,inconsistent=included.filter(f=>f.inconsistent).length;const percent=total>0?Math.max(0,Math.min(100,used/total*100)):0;
23371:     const status=inconsistent?'bad':missing?'warn':total?'ok':'muted';const label=inconsistent?'Inconsistencias detectadas':missing?'Datos financieros incompletos':total?'Datos consistentes':'Sin datos suficientes';const badge=$('d33FinanceState');badge.textContent=label;badge.className=`d33-state ${status}`;
23372:     const host=$('d33Finance');host.textContent='';const wrap=create('div','d33-finance'),kpis=create('div','d33-finance-kpis');[[money(total),'Monto total'],[money(used),'Consumido / certificado'],[money(Math.max(0,balance)),'Saldo disponible'],[`${percent.toFixed(1)} %`,'Porcentaje ejecutado']].forEach(([v,l])=>{const card=create('div','d33-fin-kpi');card.append(create('b','',v),create('span','',l));kpis.appendChild(card);});const track=create('div','d33-fin-track');const usedBar=create('div','d33-fin-used');usedBar.style.width=`${percent}%`;const rest=create('div','d33-fin-rest');rest.style.width=`${Math.max(0,100-percent)}%`;track.append(usedBar,rest);const note=create('div','d33-fin-note');note.append(create('span','',`${missing} OC sin total financiero · ${inconsistent} inconsistencia(s)`),create('span','',excluded?`${excluded} OC en moneda distinta de ARS no consolidadas`:'Consolidado ARS'));wrap.append(kpis,track,note);host.appendChild(wrap);
23373:   }
23374:   function alertSeverity(alert){const s=fold(alert.severidad||alert.categoria);if(s.includes('ROJA')||s.includes('CRIT'))return 0;if(s.includes('ALTA'))return 1;if(s.includes('AMAR')||s.includes('MEDIA')||s.includes('PROX'))return 2;return 3;}
23375:   function priorities(records,alerts,events){
23376:     const out=[];alerts.forEach(alert=>{const date=parseDate(alert.fechaRelacionada),days=date?Math.round((date-today())/86400000):null;if(date&&!inRange(date,true)&&days>=0)return;out.push({kind:alertSeverity(alert),oc:clean(alert.oc),title:clean(alert.tipoAlerta||alert.categoria||'Alerta COI'),message:clean(alert.mensaje||alert.accionSugerida||'Requiere revisión.'),provider:clean(alert.proveedor),station:clean(alert.estacion),date,days,item:alert.item,source:'alert'});});
23377:     records.filter(r=>r.financial.inconsistent).forEach(r=>out.push({kind:0,oc:r.oc,title:'Inconsistencia financiera',message:'El consumo o el saldo requieren revisión.',provider:r.provider,station:r.station,date:null,days:null,item:r.item,source:'finance'}));
23378:     const seen=new Set();return out.sort((a,b)=>a.kind-b.kind||((a.days??9999)-(b.days??9999))).filter(p=>{const key=[fold(p.oc),fold(p.title),iso(p.date)].join('|');if(seen.has(key))return false;seen.add(key);return true;}).slice(0,8);
23379:   }
23380:   function renderPriorities(records,alerts,events){const host=$('d33Priorities');host.textContent='';const list=priorities(records,alerts,events);if(!list.length){host.appendChild(create('div','d33-empty','No hay prioridades críticas para el período seleccionado.'));return;}const wrap=create('div','d33-list');list.forEach(p=>{const cls=p.kind===0?'critical':p.kind===1?'high':p.kind===3?'info':'';const row=create('article',`d33-priority ${cls}`);row.tabIndex=0;row.setAttribute('role','button');const icon=create('span','d33-priority-icon',p.kind===0?'!':p.kind===1?'↑':p.kind===2?'•':'i');const main=create('div');main.append(create('div','d33-item-title',`${p.title}${p.oc?` · OC ${p.oc}`:''}`),create('div','d33-item-meta',[p.message,p.provider,p.station].filter(Boolean).join(' · ')));const date=create('span','d33-item-date',p.date?`${iso(p.date)}${p.days!==null?` · ${p.days<0?`${Math.abs(p.days)} d vencida`:`${p.days} d`}`:''}`:'Abrir');row.append(icon,main,date);const open=()=>p.item||p.oc?openOC(p.item,p.oc):showView('vistaCentroAlertas','btnCentroAlertas');row.addEventListener('click',open);row.addEventListener('keydown',ev=>{if(ev.key==='Enter'||ev.key===' '){ev.preventDefault();open();}});wrap.appendChild(row);});host.appendChild(wrap);}
23381:   function renderActivity(timeline,events){
23382:     const host=$('d33Activity');host.textContent='';let list=timeline.slice(0,7).map(e=>({date:parseDate(e.fecha),title:e.titulo||e.tipo_evento||'Movimiento COI',oc:e.oc||'',meta:[e.proveedor,e.estacion,e.estado,e.accion_pendiente].filter(Boolean).join(' · '),raw:e,source:'timeline'}));
23383:     if(!list.length)list=events.slice().sort((a,b)=>(parseDate(b.fecha)||0)-(parseDate(a.fecha)||0)).slice(0,7).map(e=>({date:parseDate(e.fecha),title:e.tipoEvento||'Evento COI',oc:e.oc||'',meta:[e.proveedor,e.estacion,e.estado].filter(Boolean).join(' · '),raw:e,source:'calendar'}));
23384:     if(!list.length){host.appendChild(create('div','d33-empty','No hay actividad reciente para los filtros seleccionados.'));return;}const wrap=create('div','d33-list');list.forEach(item=>{const row=create('article','d33-activity');row.tabIndex=0;row.setAttribute('role','button');const icon=create('span','d33-priority-icon','↗');const main=create('div');main.append(create('div','d33-item-title',`${item.title}${item.oc?` · OC ${item.oc}`:''}`),create('div','d33-item-meta',item.meta||'Movimiento registrado'));const date=create('span','d33-item-date',item.date?iso(item.date):'');row.append(icon,main,date);const open=()=>item.source==='timeline'&&typeof window.abrirTimelineCOI==='function'?window.abrirTimelineCOI(item.oc?{oc:item.oc}:{}):(item.oc?openOC(item.raw?.item,item.oc):openCalendar(item.date,item.raw?.tipoEvento));row.addEventListener('click',open);row.addEventListener('keydown',ev=>{if(ev.key==='Enter'||ev.key===' '){ev.preventDefault();open();}});wrap.appendChild(row);});host.appendChild(wrap);
23385:   }
23386:   function documentPending(record){const item=record.item||{},s=fold(item.estadoDocumental||item.ESTADO_DOCUMENTAL||record.state);return s.includes('PENDIENT')||s.includes('INCOMPLET')||s.includes('OBSERVAD')||!clean(item.expediente||item.expte||item.EXPEDIENTE);}
23387:   function renderSecondary(records,events,alerts){
23388:     const host=$('d33Secondary');host.textContent='';const stationCount=unique(records.flatMap(r=>[...r.stations])).length,certCount=events.filter(e=>eventSeries(e)==='cert').length,expired=records.filter(r=>['expired','certifiable'].includes(r.statusKey)).length,doc=records.filter(documentPending).length,ums=(typeof unidadesMantenimiento!=='undefined'&&Array.isArray(unidadesMantenimiento))?unidadesMantenimiento:(Array.isArray(window.unidadesMantenimiento)?window.unidadesMantenimiento:[]),um=ums.filter(u=>!state.filters.station||clean(u.estacion)===state.filters.station).length;const data=[[records.length,'Total OC','orders'],[records.filter(r=>r.type==='Obra').length,'Obras','works'],[records.filter(r=>r.type==='Servicio').length,'Servicios','services'],[um,'UM','um'],[certCount,'Certificaciones','calendar'],[stationCount,'Estaciones','network'],[expired,'OC vencidas','expired'],[doc,'Documentación pendiente','documents']];data.forEach(([v,l,a])=>{const chip=create('div','d33-chip');chip.tabIndex=0;chip.setAttribute('role','button');chip.dataset.action=a;chip.append(create('b','',v),create('span','',l));host.appendChild(chip);});
23389:   }
23390:   function renderRanking(records){const host=$('d33Ranking');host.textContent='';const counts=new Map();records.forEach(r=>r.stations.forEach(s=>counts.set(s,(counts.get(s)||0)+1)));const top=[...counts.entries()].sort((a,b)=>b[1]-a[1]||a[0].localeCompare(b[0])).slice(0,5),max=Math.max(1,...top.map(x=>x[1]));if(!top.length){host.appendChild(create('div','d33-empty','Sin estaciones para el filtro seleccionado.'));return;}top.forEach(([station,value])=>{const row=create('div','d33-rank-row');row.tabIndex=0;row.setAttribute('role','button');const label=create('b','',station),track=create('div','d33-rank-track'),fill=create('div','d33-rank-fill');fill.style.width=`${value/max*100}%`;track.appendChild(fill);row.append(label,track,create('span','',value));const open=()=>openNetwork(station);row.addEventListener('click',open);row.addEventListener('keydown',ev=>{if(ev.key==='Enter'||ev.key===' '){ev.preventDefault();open();}});host.appendChild(row);});
23391:   }
23392:   function showView(viewId,buttonId){if(typeof window.mostrarVista==='function'){try{window.mostrarVista(viewId);}catch(e){}}else{document.querySelectorAll('.view').forEach(v=>v.classList.remove('active'));$(viewId)?.classList.add('active');}document.querySelectorAll('#moduleNav button').forEach(b=>b.classList.toggle('active',b.id===buttonId));}
23393:   function selectContains(selectId,text){const select=$(selectId);if(!select)return;const wanted=fold(text),option=[...select.options].find(o=>fold(o.value||o.textContent).includes(wanted));if(option)select.value=option.value;}
23394:   function resetOrderFilters(){window.__dashboardOrdersStatusFilter='';['ordenesBusqueda','ordenesFiltroTipo','ordenesFiltroEstacion','ordenesFiltroProveedor','ordenesFiltroEstado','ordenesFiltroUM','ordenesFiltroDoc','ordenesFiltroMesVenc','ordenMontoOrden'].forEach(id=>{if($(id))$(id).value='';});const context=$('uxOrderContext');if(context){context.hidden=true;context.textContent='';}}
23395:   function openOrders(opts={}){showView('vistaOrdenes','btnOrdenes');resetOrderFilters();if(opts.type&&$('ordenesFiltroTipo'))$('ordenesFiltroTipo').value=opts.type;if(opts.station)selectContains('ordenesFiltroEstacion',opts.station);if(opts.provider)selectContains('ordenesFiltroProveedor',opts.provider);if(opts.status){window.__dashboardOrdersStatusFilter=opts.status;let context=$('uxOrderContext');if(!context){context=create('div','ux-filter-context');context.id='uxOrderContext';const body=document.querySelector('#vistaOrdenes .view-body');const anchor=body?.querySelector('.ordenes-toolbar');if(anchor)anchor.insertAdjacentElement('afterend',context);}if(context){context.hidden=false;context.textContent=opts.status==='active'?'Solo OCs activas':`Filtro Dashboard: ${STATUS[opts.status]?.label||opts.status}`;}}if(opts.status==='upcoming'&&$('ordenesFiltroMesVenc'))$('ordenesFiltroMesVenc').value='proximas';if(opts.status==='expired'&&$('ordenesFiltroMesVenc'))$('ordenesFiltroMesVenc').value='vencidas';if(opts.status==='closed')selectContains('ordenesFiltroEstado','FINALIZADA');if(opts.query&&$('ordenesBusqueda'))$('ordenesBusqueda').value=opts.query;if(typeof window.renderOrdenes==='function')setTimeout(()=>window.renderOrdenes(),0);}
23396:   function openCalendar(date,type){showView('vistaCalendarioCOI','btnCalendarioCOI');const d=parseDate(date)||today();if($('coiYear'))$('coiYear').value=String(d.getFullYear());if($('coiMonth'))$('coiMonth').value=String(d.getMonth());selectContains('coiEvento',type||'');if(typeof window.renderCalendarioCOIUnificado==='function')setTimeout(()=>window.renderCalendarioCOIUnificado(),0);}
23397:   function openOC(item,oc){const key=item?.idObra||item?.idServicio||item?.idOC||item?.id||oc;if(typeof window.abrirFichaOC==='function'){try{window.abrirFichaOC(key);return;}catch(e){}}showView('vistaFichaOC','');if(typeof window.renderFichaOC==='function')window.renderFichaOC(key);}
23398:   function openNetwork(station=''){showView('vistaRed','btnRed');if($('q')){$('q').value=station;$('q').dispatchEvent(new Event('input',{bubbles:true}));}if(station&&typeof window.selectStation==='function'){const stationList=(typeof estaciones!=='undefined'&&Array.isArray(estaciones))?estaciones:(Array.isArray(window.estaciones)?window.estaciones:[]);const match=stationList.find(e=>fold(e.nombre)===fold(station));if(match)setTimeout(()=>window.selectStation(match),40);}else if(typeof window.renderMap==='function')setTimeout(()=>window.renderMap(),0);}
23399:   function render(){
23400:     try{
23401:       setVersion();if(!$('dashboardInteractivoMount'))return;if(!$('d33Filters'))skeleton();
23402:       state.records=collectRecords();renderFilters(state.records);state.filtered=filterRecords(state.records);state.events=collectEvents(state.filtered);state.alerts=collectAlerts(state.filtered);state.timeline=collectTimeline(state.filtered);
23403:       renderKpis(state.filtered,state.alerts);renderDonut(state.filtered);renderAgenda(state.events);renderFinance(state.filtered);renderPriorities(state.filtered,state.alerts,state.events);renderActivity(state.timeline,state.events);renderSecondary(state.filtered,state.events,state.alerts);renderRanking(state.filtered);
23404:       const updated=$('d33Updated');if(updated)updated.textContent=`Actualizado ${new Intl.DateTimeFormat('es-AR',{dateStyle:'short',timeStyle:'short'}).format(new Date())} · ${state.filtered.length} OC`;
23405:     }catch(error){console.error('Dashboard V58.1R33',error);const mount=$('dashboardInteractivoMount');if(mount){mount.textContent='';const box=create('div','d33-error');box.textContent='No se pudo completar el Dashboard interactivo. El resto del sistema continúa disponible. Detalle: '+String(error?.message||error);mount.appendChild(box);}}
23406:   }
23407:   function schedule(ms=80){clearTimeout(state.renderTimer);state.renderTimer=setTimeout(render,ms);}
23408:   function bind(){
23409:     if(document.__coiDashboardV33)return;document.__coiDashboardV33=true;
23410:     document.addEventListener('change',event=>{const map={d33Period:'period',d33Type:'type',d33Branch:'branch',d33Provider:'provider',d33Station:'station',d33Status:'status'};const key=map[event.target?.id];if(!key)return;state.filters[key]=event.target.value;saveFilters();schedule(40);},true);
23411:     document.addEventListener('click',event=>{
23412:       const kpi=event.target.closest('.d33-kpi');if(kpi){const action=kpi.dataset.action;if(action==='alerts')showView('vistaCentroAlertas','btnCentroAlertas');else openOrders({status:action});return;}
23413:       const chip=event.target.closest('.d33-chip');if(chip){const a=chip.dataset.action;if(a==='orders')openOrders();if(a==='works')openOrders({type:'Obra'});if(a==='services')openOrders({type:'Servicio'});if(a==='um')showView('vistaAdministracionSistema','btnAdministracionSistema');if(a==='calendar')showView('vistaCalendarioCOI','btnCalendarioCOI');if(a==='network')openNetwork();if(a==='expired')openOrders({status:'expired'});if(a==='documents')openOrders({query:'pendiente'});return;}
23414:       if(event.target.closest('#d33Clear')){state.filters={period:'30',type:'',branch:'',provider:'',station:'',status:''};saveFilters();schedule(0);return;}
23415:       if(event.target.closest('#d33OpenCalendar')){openCalendar(today(),'');return;}if(event.target.closest('#d33OpenAlerts')){showView('vistaCentroAlertas','btnCentroAlertas');return;}if(event.target.closest('#d33OpenTimeline')){if(typeof window.abrirTimelineCOI==='function')window.abrirTimelineCOI({});else showView('vistaTimelineCOI','btnTimelineCOI');return;}if(event.target.closest('#d33ViewNetwork')){openNetwork(state.filters.station);return;}
23416:       if(event.target.closest('#btnDashboard'))schedule(30);if(event.target.closest('#btnOrdenesLimpiar'))window.__dashboardOrdersStatusFilter='';
23417:     },true);
23418:     document.addEventListener('keydown',event=>{const target=event.target.closest('.d33-kpi,.d33-chip');if(target&&(event.key==='Enter'||event.key===' ')){event.preventDefault();target.click();}},true);
23419:     window.addEventListener('online',()=>schedule(250));window.addEventListener('storage',event=>{if(event.key&&/coi|orden|timeline|alert/i.test(event.key))schedule(180);});
23420:   }
23421:   function installOrdersAdapter(){const base=window.obtenerOrdenesFiltradas;if(typeof base!=='function'||base.__d33StatusAdapter)return;const fn=function(...args){const rows=base.apply(this,args);const wanted=window.__dashboardOrdersStatusFilter;if(!wanted||!Array.isArray(rows))return rows;return rows.filter(row=>{const item=row?.item||row||{};const status=statusKey({contractStatus:stateOf(row,item)});return wanted==='active'?['execution','upcoming','certifiable'].includes(status):status===wanted;});};fn.__d33StatusAdapter=true;window.obtenerOrdenesFiltradas=fn;}
23422:   function wrap(name){const base=window[name];if(typeof base!=='function'||base.__d33Wrapped)return;const fn=function(...args){const result=base.apply(this,args);Promise.resolve(result).finally(()=>schedule(80));return result;};fn.__d33Wrapped=true;window[name]=fn;}
23423:   function init(){readFilters();skeleton();bind();installOrdersAdapter();['updateKpis','renderDashboardInteractivoExtra','renderDashboardEjecutivo','recargarDatosDesdeSupabase','cargarOrdenesPrincipal','adminRefreshAll'].forEach(wrap);render();setTimeout(render,900);setTimeout(render,2600);}
23424:   window.renderDashboardInteractivo=render;
23425:   window.dashboardFilters=state.filters;
23426:   window.COI=Object.assign(window.COI||{},{dashboard:{version:VERSION,render,filters:state.filters,openOrders,openCalendar}});
23427:   if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
23428: })();
23429: </script>
23430: 
23431: 
23432: <script id="coi-v581r34-experiencia-visual-script">
23433: (()=>{
23434:   'use strict';
23435:   const VERSION='V58.1R39-FINANZAS-SUPABASE-2A';
23436:   const MOTION_KEY='coi.visual.motion.enabled';
23437:   const $=id=>document.getElementById(id);
23438:   const reduced=()=>window.matchMedia&&window.matchMedia('(prefers-reduced-motion: reduce)').matches;
23439:   function updateVersion(){
23440:     ['appVersionHeader','headerVersionOperativa'].forEach(id=>{const el=$(id);if(el)el.textContent=VERSION;});
23441:     try{if(typeof window.APP_VERSION!=='undefined')window.APP_VERSION=VERSION;}catch(_e){}
23442:     if(window.COI)window.COI.version=VERSION;
23443:   }
23444:   function createRailSignature(){
23445:     const wrap=document.createElement('div');wrap.className='coi-rail-signature';wrap.setAttribute('aria-label','Identidad visual de la red ferroviaria');
23446:     const ns='http://www.w3.org/2000/svg';const svg=document.createElementNS(ns,'svg');svg.setAttribute('viewBox','0 0 180 30');svg.setAttribute('aria-hidden','true');
23447:     const glow=document.createElementNS(ns,'path');glow.setAttribute('d','M4 20 C32 20 33 7 59 7 H102 C126 7 130 20 176 20');glow.setAttribute('class','coi-rail-track-glow');
23448:     const track=document.createElementNS(ns,'path');track.setAttribute('d','M4 20 C32 20 33 7 59 7 H102 C126 7 130 20 176 20');track.setAttribute('class','coi-rail-track');
23449:     svg.append(glow,track);
23450:     [[18,20],[59,7],[102,7],[160,20]].forEach(([cx,cy])=>{const c=document.createElementNS(ns,'circle');c.setAttribute('cx',cx);c.setAttribute('cy',cy);c.setAttribute('r','4.5');c.setAttribute('class','coi-rail-node');svg.appendChild(c);});
23451:     const label=document.createElement('span');label.textContent='Red, contratos y decisiones en una sola vista';wrap.append(svg,label);return wrap;
23452:   }
23453:   function button(id,label,title){const b=document.createElement('button');b.type='button';b.id=id;b.className='coi-visual-btn';b.textContent=label;b.title=title;return b;}
23454:   function motionEnabled(){if(reduced())return false;try{return localStorage.getItem(MOTION_KEY)!=='0';}catch(_e){return true;}}
23455:   function applyMotion(enabled){document.body.classList.toggle('coi-motion-off',!enabled);const b=$('coiToggleMotion');if(b){b.setAttribute('aria-pressed',String(enabled));b.textContent=enabled?'Movimiento activo':'Movimiento pausado';b.title=enabled?'Pausar animaciones decorativas':'Activar animaciones decorativas';}try{localStorage.setItem(MOTION_KEY,enabled?'1':'0');}catch(_e){}}
23456:   function toggleFocus(){const active=document.body.classList.toggle('dashboard-focus-mode');const b=$('coiFocusMode');if(b){b.setAttribute('aria-pressed',String(active));b.textContent=active?'Salir de modo foco':'Modo foco';}if(active)window.scrollTo({top:0,behavior:motionEnabled()?'smooth':'auto'});}
23457:   function safeName(value){return String(value||'grafico-coi').normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-zA-Z0-9_-]+/g,'-').replace(/^-+|-+$/g,'').toLowerCase();}
23458:   function exportSvg(svg,name){
23459:     if(!svg){if(typeof window.mostrarToastCOI==='function')window.mostrarToastCOI('El gráfico todavía no está disponible.','warning');return;}
23460:     try{
23461:       const ns='http://www.w3.org/2000/svg',clone=svg.cloneNode(true),view=clone.getAttribute('viewBox')||'0 0 900 500',parts=view.split(/\s+/).map(Number),w=parts[2]||900,h=parts[3]||500;
23462:       clone.setAttribute('xmlns',ns);clone.setAttribute('width',String(w));clone.setAttribute('height',String(h));
23463:       const bg=document.createElementNS(ns,'rect');bg.setAttribute('x',String(parts[0]||0));bg.setAttribute('y',String(parts[1]||0));bg.setAttribute('width',String(w));bg.setAttribute('height',String(h));bg.setAttribute('fill','#ffffff');clone.insertBefore(bg,clone.firstChild);
23464:       const style=document.createElementNS(ns,'style');style.textContent='.d33-axis{stroke:#d8e4ed;stroke-width:1}.d33-axis-label{fill:#667887;font:10px Segoe UI,Arial}.d33-month-label{fill:#34495e;font:800 11px Segoe UI,Arial;text-anchor:middle}.d33-donut-center-big{fill:#17384f;font:900 26px Segoe UI,Arial;text-anchor:middle}.d33-donut-center-small{fill:#667887;font:900 11px Segoe UI,Arial;text-anchor:middle}';clone.insertBefore(style,bg.nextSibling);
23465:       const xml=new XMLSerializer().serializeToString(clone),blob=new Blob([xml],{type:'image/svg+xml;charset=utf-8'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=`${safeName(name)}-${new Date().toISOString().slice(0,10)}.svg`;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1200);
23466:       if(typeof window.mostrarToastCOI==='function')window.mostrarToastCOI('Gráfico SVG exportado.','ok');
23467:     }catch(error){console.error('COI export SVG',error);if(typeof window.mostrarToastCOI==='function')window.mostrarToastCOI('No se pudo exportar el gráfico.','error');}
23468:   }
23469:   function addExportButton(card,title,targetId){
23470:     if(!card||card.querySelector(`[data-coi-export="${targetId}"]`))return;const head=card.querySelector('.d33-card-head');if(!head)return;const b=document.createElement('button');b.type='button';b.className='coi-export-svg';b.dataset.coiExport=targetId;b.textContent='Exportar SVG';b.title=`Descargar ${title} como archivo SVG`;b.addEventListener('click',()=>exportSvg(document.querySelector(`#${targetId} svg`),title));head.appendChild(b);
23471:   }
23472:   function decorate(){
23473:     updateVersion();const mount=$('dashboardInteractivoMount'),head=mount?.querySelector('.d33-head');if(!mount||!head)return false;
23474:     if(!head.querySelector('.coi-rail-signature'))head.querySelector('div')?.appendChild(createRailSignature());
23475:     if(!head.querySelector('.coi-visual-controls')){const controls=document.createElement('div');controls.className='coi-visual-controls';const motion=button('coiToggleMotion','Movimiento activo','Pausar animaciones decorativas');motion.setAttribute('aria-pressed','true');motion.addEventListener('click',()=>applyMotion(document.body.classList.contains('coi-motion-off')));const focus=button('coiFocusMode','Modo foco','Maximizar el Dashboard para presentación');focus.setAttribute('aria-pressed','false');focus.addEventListener('click',toggleFocus);controls.append(motion,focus);head.appendChild(controls);}
23476:     const cards=[...mount.querySelectorAll('.d33-card')];cards.forEach(card=>{const title=card.querySelector('h4')?.textContent||'';if(title.includes('Estado contractual'))addExportButton(card,'estado-contractual-oc','d33Donut');if(title.includes('Agenda COI'))addExportButton(card,'agenda-coi-seis-meses','d33Agenda');});
23477:     applyMotion(motionEnabled());return true;
23478:   }
23479:   function springPress(el){if(!el||document.body.classList.contains('coi-motion-off')||reduced()||typeof el.animate!=='function')return;el.animate([{transform:getComputedStyle(el).transform},{transform:'scale(.975)'},{transform:getComputedStyle(el).transform}],{duration:360,easing:'cubic-bezier(.2,1.35,.3,1)'});}
23480:   function bind(){